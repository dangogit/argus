# V2

V2 is the product. The installed CLI is `argus`, backed by
`argus.v2.cli:main`.

Start here:

```bash
cp argus.v2.example.yaml argus.yaml
export ARGUS_CONFIG="$PWD/argus.yaml"
export ARGUS_CONFIG_V2="$PWD/argus.yaml"
argus validate
argus doctor
```
